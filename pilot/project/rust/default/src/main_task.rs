use crate::{async_util::either, print, println, time, VARS};
use futures::future::{self, Either};
use pilot_types::var::{VarChange, VarProps};
use time::{current_time, wait_until, SECOND};

pub async fn main_task() {

    // Application loop
    loop {

        
    }
}
